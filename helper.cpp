#include <iostream>

#include "helper.h"

using namespace std;
/*
bool input_check (string origin, string destination)
{
  if(static_cast<char>(origin[0]) > 'H' || < 'A')
    return false;

  if(atoi(origin[1]) > 8 || < 1)
    return false;

  if(static_cast<char>(destination[0]) > 'H' || < 'A')
    return false;

  if(atoi(destination[1]) > 8 || < 1)
    return false;

  return true;
}
*/
